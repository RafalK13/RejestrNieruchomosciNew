<UserControl x:Class="RejestrNieruchomosciNew.View.UserControl_Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RejestrNieruchomosciNew.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical" >
        <Button Height="50"
                Margin="13"
                Command="{Binding buttonClick1}"
                IsEnabled="{Binding button1}"/>

        <Button Height="50"
                Margin="13"
                Command="{Binding buttonClick2}"
                IsEnabled="{Binding button2}"/>
    </StackPanel>
</UserControl>
